default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: myblog_development
  host: <%= ENV.fetch("DB_HOST") { "localhost" } %>
  port: <%= ENV.fetch("DB_PORT") { 5432 } %>
  username: <%= ENV.fetch("DB_USERNAME") { "postgres" } %>
  password: <%= ENV.fetch("DB_PASSWORD") { "" } %>

test:
  <<: *default
  database: myblog_test
  host: <%= ENV.fetch("DB_HOST") { "localhost" } %>
  port: <%= ENV.fetch("DB_PORT") { 5432 } %>
  username: <%= ENV.fetch("DB_USERNAME") { "postgres" } %>
  password: <%= ENV.fetch("DB_PASSWORD") { "" } %>

production:
  primary: &primary_production
    <<: *default
    host: <%= Rails.application.credentials.dig(:database, :hostname) %>
    port: <%= Rails.application.credentials.dig(:database, :port) %>
    database: <%= Rails.application.credentials.dig(:database, :database) %>
    username: <%= Rails.application.credentials.dig(:database, :username) %>
    password: <%= Rails.application.credentials.dig(:database, :password) %>
  # cache/queue/cable are extra connections to the SAME physical database as primary.
  # database_tasks: false tells Rails not to manage their schema separately — the Solid
  # tables are created by the primary migration (db/migrate) and live in the shared DB.
  cache:
    <<: *primary_production
    database_tasks: false
  queue:
    <<: *primary_production
    database_tasks: false
  cable:
    <<: *primary_production
    database_tasks: false
